const locations = require('./lib/location')
const jobPosts = require('./lib/job_post')
const enquiries = require('./lib/enquiry')
const counts = require('./lib/counts')

module.exports = {
  locations,
  jobPosts,
  enquiries,
  counts
}
